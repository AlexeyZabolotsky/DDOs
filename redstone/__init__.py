"""Редстоун на Python: мир блоков, управляемый сообщениями.

Каждое действие (генерация, уничтожение, сохранение, стирание, передача,
блокировка, выделение, соединение, разъединение, перемещение) — это
сообщение формата:

    {
        "состояние": <команда>,
        "координата": (x, y, z),
        "данные": {...любая вложенность...},
    }

Сообщения создаются как кликами мыши (см. redstone.ui), так и данными
внутри самих блоков (поле "программа" в памяти блока).
"""

from redstone.core import (
    Block,
    World,
    Message,
    СОСТОЯНИЯ,
    сообщение,
)
from redstone.codex3d_bridge import Codex3DBridge

__all__ = ["Block", "World", "Message", "СОСТОЯНИЯ", "сообщение", "Codex3DBridge"]
